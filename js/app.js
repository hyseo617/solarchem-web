// app.js
// 전체 프로그램 실행 및 사용자 입력 처리

document.addEventListener('DOMContentLoaded', () => {
  initApp();
});

function initApp() {
  // 위치·시각 입력 폼 초기화
  // 화학물질 선택 목록 초기화
  // 모드 선택(Instant Analysis / Time-course Simulation) 초기화
}

function handleModeSelection(mode) {
  // mode: 'instant' | 'timecourse'
}
